export function runWindowsCommand(): void { throw new Error("runWindowsCommand not yet wired in TS"); }
export function runNotepad(): void { throw new Error("runNotepad not yet wired in TS"); }
export function runGoogleChat(): void { throw new Error("runGoogleChat not yet wired in TS"); }
export function runHubstaff(): void { throw new Error("runHubstaff not yet wired in TS"); }
export function runMail(): void { throw new Error("runMail not yet wired in TS"); }
export function runOutlookMail(): void { throw new Error("runOutlookMail not yet wired in TS"); }
export function runXamppStart(): void { throw new Error("runXamppStart not yet wired in TS"); }
export function runXamppStop(): void { throw new Error("runXamppStop not yet wired in TS"); }

