{
  "name": "@ramkumarbedia/xqme",
  "version": "4.0.4",
  "description": "Cross-platform developer command-line toolkit",
  "main": "index.js",
  "bin": {
    "qme": "index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "cli",
    "node",
    "cross-platform",
    "tool"
  ],
  "files": [
    "index.js",
    "src",
    "completions"
  ],
  "scripts": {
    "test": "node --test",
    "check": "npm test && npm run lint && npm run format:check",
    "prepublishOnly": "npm run check",
    "version:check": "node scripts/check-version.js",
    "lint": "eslint index.js src test",
    "format": "prettier --write src/commands src/services src/errors.js test",
    "format:check": "prettier --check src/commands src/services src/errors.js test"
  },
  "dependencies": {
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "eslint": "^10.8.0",
    "prettier": "^3.9.6"
  }
}
